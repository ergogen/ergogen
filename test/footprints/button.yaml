points.zones.matrix:
pcbs.pcb.footprints:

  # front
  - what: button
    params:
      from: from
      to: to

  # back
  - what: button
    params:
      from: from
      to: to
      side: B
    adjust.shift: [10, 0]

  # PTS636_SK43_LFS variant
  - what: button
    params:
      from: from
      to: to
      variant: PTS636_SK43_LFS
    adjust.shift: [0, 10]
